{
  "name": "client",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "build": "bun build --target=bun --outdir=dist index.ts"
  },
  "dependencies": {
    "@repo/models": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
