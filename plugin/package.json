{
  "name": "plugin",
  "version": "1.0.0",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "build": "bun build --target=bun --outdir=dist index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/models": "workspace:*"
  }
}
