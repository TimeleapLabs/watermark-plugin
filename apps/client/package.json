{
  "name": "@apps/client",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build --target=bun --outdir=dist src/index.ts"
  },
  "dependencies": {
    "@shared/models": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
